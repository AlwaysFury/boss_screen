<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boss.client.dao.TagDao">


    <select id="tagCount" resultType="java.lang.Integer">
        SELECT
            count(*)
        FROM
            tb_tag as s
        <where>
            s.id is not null
            <if test="condition.tag_name != null">
                and s.tag_name = #{condition.tag_name}
            </if>
            <if test="condition.tag_type != null">
                and s.tag_type = #{condition.tag_type}
            </if>
            and s.tag_type != 'AUTO'
        </where>
    </select>


    <select id="tagList" resultType="com.boss.client.vo.TagVO">
        SELECT
            id,
            tag_name,
            tag_type
        FROM
            tb_tag as s
        <where>
            s.id is not null
            <if test="condition.tag_name != null">
                and s.tag_name = #{condition.tag_name}
            </if>
            <if test="condition.tag_type != null">
                and s.tag_type = #{condition.tag_type}
            </if>
            and s.tag_type != 'AUTO'
        </where>
        LIMIT #{current},#{size}
    </select>


    <select id="getTagListByItemOrImgId" resultType="com.boss.common.enities.Tag">
        SELECT
            s.tag_name
        FROM
            tb_tag as s
        left join tb_productorimg_tag as t on t.tag_id = s.id
        <where>
            s.id is not null
            and t.itemOrImg_id = #{itemOrImg_id}
        </where>
    </select>

</mapper>
